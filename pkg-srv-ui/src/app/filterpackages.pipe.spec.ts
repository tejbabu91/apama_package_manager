import { FilterpackagesPipe } from './filterpackages.pipe';

describe('FilterpackagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpackagesPipe();
    expect(pipe).toBeTruthy();
  });
});
