package:
  - name: "..."
  - description: "..."
  - version: "..."
  - repository: "..."
  - tags:
    - "..."
  - extra-dep: false # true if package has dependencies else false
  - extra-mon: false # true if package require monitors to be injected first else false
  - extra-evt: false # true if package require events to be injected first else false
  - connectivity-plugin: false #true if package require connectivity plugins else false

extra-deps:
  - "..." #package name

extra-mons:
  - "..." # list of monitors to be injected

extra-evts:
  - "..." # list of events to be injected

connectivity-plugins:
  pluginName: "..."
    libraryName: "..."
    class: "..."